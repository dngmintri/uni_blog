# ========================
# UniBlog Backend Environment Variables
# ========================
# Copy file này thành .env và điền các giá trị thực

# Database Configuration
DB_SERVER=localhost
DB_PORT=3306
DB_NAME=uni_blog
DB_USER=root
DB_PASSWORD=your_mysql_password

# JWT Configuration
JWT_SECRET_KEY=your_super_secret_key_minimum_32_characters_long
JWT_ISSUER=UniBlogAPI
JWT_AUDIENCE=UniBlogClient
JWT_EXPIRATION_MINUTES=60

# CORS Origins (separated by semicolon)
CORS_ORIGINS=https://localhost:5001;http://localhost:5000

# Environment
ASPNETCORE_ENVIRONMENT=Development

